def find_uniq(arr):
    if arr[0] == arr[1]:
        same = arr[0]
    elif arr[0] == arr[2]:
        same = arr[0]
    else:
        return arr[0]
    for i in arr:
        if i != same:
            return i
