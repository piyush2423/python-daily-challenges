def pig_it(text):
    words = text.split()
    result = []
    for word in words:
        if word.isalpha():  # Only transform alphabetic words
            result.append(word[1:] + word[0] + "ay")
        else:  # Leave punctuation untouched
            result.append(word)
    return " ".join(result)